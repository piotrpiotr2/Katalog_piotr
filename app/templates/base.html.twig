<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Welcome!{% endblock %}</title>

    <!-- CRUCIAL for responsiveness -->
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <link rel="icon"
          href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">

    {% block stylesheets %}
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
              rel="stylesheet"
              integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
              crossorigin="anonymous">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css"
              rel="stylesheet">
    {% endblock %}
</head>

<body>
<nav class="navbar navbar-expand-md bg-body-tertiary mb-3">
    <div class="container">
        <a class="navbar-brand" href="{{ path('album_index')|default('#') }}">{{ 'title.album_catalogue'|trans }}</a>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mainNav"
                aria-controls="mainNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div id="mainNav" class="collapse navbar-collapse" style="justify-content: end">

            <div class="d-flex align-items-center gap-2" >
                {% include 'default/_flash_messages.html.twig' ignore missing %}
                {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                    <a class="link-body-emphasis text-decoration-none" href="{{ url('profile') }}">
                        {{ app.user.email }}
                    </a>
                    <span class="text-secondary d-none d-md-inline">|</span>
                    <a class="btn btn-sm btn-outline-secondary" href="{{ url('app_logout') }}">
                        {{ 'action.logout'|trans }}
                    </a>
                {% else %}
                    {% if app.request.attributes.get('_route') != 'app_login' %}
                        <a class="btn btn-sm btn-primary" href="{{ url('app_login') }}">{{ 'action.login'|trans }}</a>
                    {% else %}
                        <a class="btn btn-sm btn-primary" href="{{ url('app_register') }}">{{ 'action.register'|trans }}</a>
                    {% endif %}
                {% endif %}
            </div>
        </div>
    </div>
</nav>

<main role="main" class="container mb-5">
    {% block body %}{% endblock %}
</main>

{% block javascripts %}
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
            integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
            crossorigin="anonymous"></script>
{% endblock %}
</body>
</html>
