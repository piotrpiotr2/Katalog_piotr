{% include 'default/_form.html.twig' with {
    page_title: 'title.album_edit'|trans({'%id%': albums.id|default('')}),
    form: form,
    submit_label: 'action.edit'|trans,
    back_to_list_path: 'album_index',
} %}